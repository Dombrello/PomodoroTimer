<Window x:Class="PomodoroTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PomodoroTimer"
        mc:Ignorable="d"
        Title="Pomodoro Timer" Height="300" Width="400">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Pozostały czas:" FontSize="16"/>
            <TextBox x:Name="timeTextBox" Text="25:00" FontSize="24" IsReadOnly="True" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10"/>
            <StackPanel Orientation="Horizontal" Margin="0,20">
                <Button Content="Start" Click="startButton_Click" Margin="5"/>
                <Button Content="Stop" Click="stopButton_Click" Margin="5"/>
                <Button Content="Reset" Click="resetButton_Click" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
